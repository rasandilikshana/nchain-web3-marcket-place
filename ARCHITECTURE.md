# 🏗️ nchain Web3 Marketplace - Architecture

> Complete system architecture showing how the blockchain, marketplace, and all components work together

---

## 📐 High-Level Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          USER INTERACTION LAYER                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌──────────────┐         ┌──────────────┐         ┌──────────────┐        │
│  │   Browser    │         │   Browser    │         │   Browser    │        │
│  │              │         │              │         │              │        │
│  │  User Alice  │         │   User Bob   │         │  User Carol  │        │
│  └──────┬───────┘         └──────┬───────┘         └──────┬───────┘        │
│         │                        │                        │                 │
└─────────┼────────────────────────┼────────────────────────┼─────────────────┘
          │                        │                        │
          │ HTTP/HTTPS             │ HTTP/HTTPS             │ HTTP/HTTPS
          │                        │                        │
┌─────────▼────────────────────────▼────────────────────────▼─────────────────┐
│                         FRONTEND LAYER (React)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    React Application (Vite)                          │   │
│  │                       Port: 5173                                     │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                       │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │  HomePage    │  │ Marketplace  │  │  MintPage    │              │   │
│  │  │              │  │    Page      │  │              │              │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │   │
│  │                                                                       │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │  MyGemsPage  │  │ GemDetail    │  │   Wallet     │              │   │
│  │  │              │  │    Page      │  │  Component   │              │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │   │
│  │                                                                       │   │
│  │  ┌───────────────────────────────────────────────────────┐          │   │
│  │  │          State Management (React Context)              │          │   │
│  │  │  - WalletContext  - GemContext  - MarketplaceContext  │          │   │
│  │  └───────────────────────────────────────────────────────┘          │   │
│  │                                                                       │   │
│  └───────────────────────────┬───────────────────────────────────────────┘ │
│                              │                                             │
└──────────────────────────────┼─────────────────────────────────────────────┘
                               │
                               │ REST API Calls
                               │ (JSON)
┌──────────────────────────────▼─────────────────────────────────────────────┐
│                        BACKEND API LAYER (Node.js)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                   Express.js Server (TypeScript)                     │   │
│  │                          Port: 3000                                  │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │                                                                       │   │
│  │  ┌────────────────────────  API Routes  ──────────────────────────┐ │   │
│  │  │                                                                  │ │   │
│  │  │  POST /api/auth/login          - Wallet Authentication          │ │   │
│  │  │  GET  /api/gems                - List all gems                  │ │   │
│  │  │  GET  /api/gems/:id            - Get gem details                │ │   │
│  │  │  POST /api/gems/mint           - Mint new gem                   │ │   │
│  │  │  GET  /api/gems/owner/:address - Gems by owner                  │ │   │
│  │  │                                                                  │ │   │
│  │  │  GET  /api/marketplace/listings     - Active listings           │ │   │
│  │  │  POST /api/marketplace/list         - Create listing            │ │   │
│  │  │  POST /api/marketplace/buy          - Purchase gem              │ │   │
│  │  │  DELETE /api/marketplace/listing/:id - Cancel listing           │ │   │
│  │  │                                                                  │ │   │
│  │  │  GET  /api/users/:address          - User profile               │ │   │
│  │  │  GET  /api/users/:address/gems     - User's collection          │ │   │
│  │  │                                                                  │ │   │
│  │  └──────────────────────────────────────────────────────────────────┘ │   │
│  │                                                                       │   │
│  │  ┌────────────────────────  Services  ─────────────────────────────┐ │   │
│  │  │                                                                  │ │   │
│  │  │  • GemService      - Gem business logic                         │ │   │
│  │  │  • MarketService   - Marketplace operations                     │ │   │
│  │  │  • WalletService   - Authentication & signing                   │ │   │
│  │  │  • BlockchainService - nchain communication                     │ │   │
│  │  │                                                                  │ │   │
│  │  └──────────────────────────────────────────────────────────────────┘ │   │
│  │                                                                       │   │
│  └───────┬───────────────────────────────────┬───────────────────────────┘ │
│          │                                   │                             │
└──────────┼───────────────────────────────────┼─────────────────────────────┘
           │                                   │
           │ HTTP API Calls                    │ MongoDB Queries
           │ (Port 8080)                       │ (Port 27017)
           │                                   │
     ┌─────▼─────┐                      ┌──────▼──────┐
     │           │                      │             │
     │  nchain   │                      │  MongoDB    │
     │Blockchain │                      │  Database   │
     │  Node     │                      │             │
     │           │                      │  (Caching)  │
     └─────┬─────┘                      └─────────────┘
           │                                   ▲
┌──────────▼───────────────────────────────────┼─────────────────────────────┐
│                    BLOCKCHAIN LAYER (Rust)   │                              │
├──────────────────────────────────────────────┼─────────────────────────────┤
│                                              │                              │
│  ┌───────────────────────────────────────────┼──────────────────────────┐  │
│  │                  nchain Blockchain Node   │                          │  │
│  │                    (Rust + libp2p)        │                          │  │
│  ├───────────────────────────────────────────┼──────────────────────────┤  │
│  │                                           │                          │  │
│  │  ┌─────────────────────────────────────┐ │                          │  │
│  │  │      HTTP API Server (Port 8080)    │─┘                          │  │
│  │  │                                     │                            │  │
│  │  │  • /api/blocks         - Blocks    │                            │  │
│  │  │  • /api/transactions   - Txs       │                            │  │
│  │  │  • /api/wallet         - Wallet    │                            │  │
│  │  │  • /api/contract       - Contracts │                            │  │
│  │  └─────────────────────────────────────┘                            │  │
│  │                                                                      │  │
│  │  ┌──────────────────────  Core Components  ─────────────────────┐  │  │
│  │  │                                                               │  │  │
│  │  │  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐ │  │  │
│  │  │  │   Blockchain  │  │  Transaction  │  │   Mempool        │ │  │  │
│  │  │  │     Chain     │  │    Manager    │  │  (Pending Txs)   │ │  │  │
│  │  │  └───────────────┘  └───────────────┘  └──────────────────┘ │  │  │
│  │  │                                                               │  │  │
│  │  │  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐ │  │  │
│  │  │  │  PoW Mining   │  │   Consensus   │  │  Block           │ │  │  │
│  │  │  │    Engine     │  │   Validator   │  │  Validator       │ │  │  │
│  │  │  └───────────────┘  └───────────────┘  └──────────────────┘ │  │  │
│  │  │                                                               │  │  │
│  │  │  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐ │  │  │
│  │  │  │   WASM VM     │  │  Smart        │  │   State          │ │  │  │
│  │  │  │   (Wasmer)    │  │  Contracts    │  │   Manager        │ │  │  │
│  │  │  └───────────────┘  └───────────────┘  └──────────────────┘ │  │  │
│  │  │                                                               │  │  │
│  │  └───────────────────────────────────────────────────────────────┘  │  │
│  │                                                                      │  │
│  │  ┌────────────────────  Storage Layer  ──────────────────────────┐  │  │
│  │  │                                                                │  │  │
│  │  │  • RocksDB     - Block storage, chain data                    │  │  │
│  │  │  • SQLite      - Transaction indexing, quick queries          │  │  │
│  │  │  • blockchain.db - Main database file                         │  │  │
│  │  │                                                                │  │  │
│  │  └────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                      │  │
│  └──────────────────────────┬───────────────────────────────────────────┘  │
│                             │                                              │
└─────────────────────────────┼──────────────────────────────────────────────┘
                              │
                              │ libp2p P2P Network
                              │ (Port 9000)
┌─────────────────────────────▼──────────────────────────────────────────────┐
│                        P2P NETWORK LAYER                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                               │
│     ┌──────────┐          ┌──────────┐          ┌──────────┐               │
│     │  Peer 1  │◄────────►│  Peer 2  │◄────────►│  Peer 3  │               │
│     │  Node    │          │  Node    │          │  Node    │               │
│     └──────────┘          └──────────┘          └──────────┘               │
│          ▲                      ▲                      ▲                     │
│          │                      │                      │                     │
│          └──────────────────────┼──────────────────────┘                     │
│                                 │                                            │
│                     Block Propagation & Sync                                 │
│                     Transaction Broadcasting                                 │
│                     Peer Discovery                                           │
│                                                                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Minting a New Gem

```
┌─────────┐                                                    ┌──────────────┐
│  User   │                                                    │   Frontend   │
│ (Alice) │                                                    │   (React)    │
└────┬────┘                                                    └──────┬───────┘
     │                                                                │
     │ 1. Clicks "Mint Gem"                                          │
     │ ───────────────────────────────────────────────────────────►  │
     │                                                                │
     │                                                                │ 2. POST /api/gems/mint
     │                                                                │    { color, rarity, attributes }
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Backend   │
     │                                                         │   (Node.js) │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 3. Validate request
     │                                                                │ 4. Sign transaction
     │                                                                │
     │                                                                │ 5. POST /api/contract/execute
     │                                                                │    (Mint contract call)
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │  nchain     │
     │                                                         │  Blockchain │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 6. Create transaction
     │                                                                │ 7. Add to mempool
     │                                                                │ 8. Mine block (PoW)
     │                                                                │ 9. Execute WASM contract
     │                                                                │ 10. Update state
     │                                                                │
     │                                                                │ 11. Return tx_hash
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Backend   │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 12. Save to MongoDB
     │                                                                │ 13. Index gem data
     │                                                                │
     │                                                                │ 14. Response: { gemId, txHash }
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Frontend  │
     │  15. Shows success message                             └──────┬──────┘
     │  ◄────────────────────────────────────────────────────────────┘
     │  "Gem minted! ID: gem_123"
     │
▼
```

### 2. Buying a Gem from Marketplace

```
┌─────────┐                                                    ┌──────────────┐
│  User   │                                                    │   Frontend   │
│  (Bob)  │                                                    │   (React)    │
└────┬────┘                                                    └──────┬───────┘
     │                                                                │
     │ 1. Browse marketplace                                         │
     │ ───────────────────────────────────────────────────────────►  │
     │                                                                │
     │                                                                │ 2. GET /api/marketplace/listings
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Backend   │
     │                                         3. Response ◄───┤             │
     │                                           [listing1,    │             │
     │  4. Shows available gems                  listing2...]  └─────────────┘
     │  ◄────────────────────────────────────────────────────
     │
     │ 5. Clicks "Buy Gem #42"                                       │
     │ ───────────────────────────────────────────────────────────►  │
     │                                                                │
     │                                                                │ 6. POST /api/marketplace/buy
     │                                                                │    { listingId, buyerAddress }
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Backend   │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 7. Check listing validity
     │                                                                │ 8. Check buyer balance
     │                                                                │ 9. Create purchase tx
     │                                                                │
     │                                                                │ 10. Execute marketplace contract
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │  nchain     │
     │                                                         │  Blockchain │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 11. Verify transaction
     │                                                                │ 12. Transfer ownership
     │                                                                │ 13. Transfer payment
     │                                                                │ 14. Update NFT state
     │                                                                │ 15. Mine block
     │                                                                │
     │                                                                │ 16. Emit events:
     │                                                                │     - GemTransferred
     │                                                                │     - ListingCompleted
     │                                                                ▼
     │                                                         ┌─────────────┐
     │                                                         │   Backend   │
     │                                                         └──────┬──────┘
     │                                                                │
     │                                                                │ 17. Update MongoDB
     │                                                                │     - Update gem owner
     │                                                                │     - Remove listing
     │                                                                │     - Record transaction
     │                                                                │
     │  18. Success! Gem now in "My Gems"                            │ 19. Response: { success, txHash }
     │  ◄────────────────────────────────────────────────────────────┘
     │
▼
```

---

## 🧩 Component Interactions

### Smart Contract Execution Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                       Smart Contract Lifecycle                        │
└──────────────────────────────────────────────────────────────────────┘

    ┌─────────────┐
    │  Contract   │
    │   Call      │
    │ (via API)   │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  Create     │
    │ Transaction │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Add to    │
    │  Mempool    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐         ┌──────────────┐
    │   Miner     │────────►│  Find Nonce  │
    │ Picks TX    │         │     (PoW)    │
    └──────┬──────┘         └──────────────┘
           │
           ▼
    ┌─────────────┐
    │   Create    │
    │    Block    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  Validate   │
    │    Block    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐         ┌──────────────┐
    │  Execute    │────────►│  Load WASM   │
    │  Contract   │         │    Binary    │
    └──────┬──────┘         └──────────────┘
           │
           ▼
    ┌─────────────┐
    │   Run in    │
    │  Wasmer VM  │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Update    │
    │    State    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Emit      │
    │   Events    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │  Broadcast  │
    │   to P2P    │
    └──────┬──────┘
           │
           ▼
    ┌─────────────┐
    │   Return    │
    │   Result    │
    └─────────────┘
```

### Authentication & Wallet Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                      Wallet Authentication Flow                       │
└──────────────────────────────────────────────────────────────────────┘

    User                Frontend              Backend            Blockchain
     │                      │                     │                    │
     │  1. Click "Connect"  │                     │                    │
     ├─────────────────────►│                     │                    │
     │                      │                     │                    │
     │  2. Enter address    │                     │                    │
     │     or generate new  │                     │                    │
     ├─────────────────────►│                     │                    │
     │                      │                     │                    │
     │                      │  3. Generate keypair (if new)            │
     │                      │     - Private Key                        │
     │                      │     - Public Key                         │
     │                      │     - Address (hash)                     │
     │                      │                     │                    │
     │                      │  4. POST /api/auth/login                 │
     │                      │     { address }     │                    │
     │                      ├────────────────────►│                    │
     │                      │                     │                    │
     │                      │                     │  5. GET /api/wallet/{address}
     │                      │                     ├───────────────────►│
     │                      │                     │                    │
     │                      │                     │  6. Wallet data    │
     │                      │                     │◄───────────────────┤
     │                      │                     │                    │
     │                      │  7. Create session  │                    │
     │                      │     Generate JWT    │                    │
     │                      │                     │                    │
     │                      │  8. Response:       │                    │
     │                      │     { token, user } │                    │
     │                      │◄────────────────────┤                    │
     │                      │                     │                    │
     │  9. Store in context │                     │                    │
     │     - User address   │                     │                    │
     │     - JWT token      │                     │                    │
     │     - Private key    │                     │                    │
     │                      │                     │                    │
     │  10. Show "Connected"│                     │                    │
     │◄─────────────────────┤                     │                    │
     │                      │                     │                    │
     │                                                                  │
     │  All future requests include JWT in Authorization header        │
     │                                                                  │
```

---

## 📡 API Endpoint Reference

### Frontend → Backend APIs

| Method | Endpoint | Purpose | Request | Response |
|--------|----------|---------|---------|----------|
| **Authentication** |
| POST | `/api/auth/login` | Wallet login | `{ address }` | `{ token, user }` |
| **Gems** |
| GET | `/api/gems` | List all gems | - | `[{ id, owner, attributes }]` |
| GET | `/api/gems/:id` | Get gem details | - | `{ id, owner, rarity, ... }` |
| POST | `/api/gems/mint` | Mint new gem | `{ color, rarity }` | `{ gemId, txHash }` |
| GET | `/api/gems/owner/:address` | User's gems | - | `[{ gem1 }, { gem2 }]` |
| **Marketplace** |
| GET | `/api/marketplace/listings` | Active listings | - | `[{ listingId, gem, price }]` |
| POST | `/api/marketplace/list` | Create listing | `{ gemId, price }` | `{ listingId, txHash }` |
| POST | `/api/marketplace/buy` | Purchase gem | `{ listingId }` | `{ success, txHash }` |
| DELETE | `/api/marketplace/listing/:id` | Cancel listing | - | `{ success }` |
| **Users** |
| GET | `/api/users/:address` | User profile | - | `{ address, balance, gems }` |
| GET | `/api/users/:address/gems` | User collection | - | `[{ gem1 }, { gem2 }]` |

### Backend → Blockchain APIs

| Method | Endpoint | Purpose | Request | Response |
|--------|----------|---------|---------|----------|
| **Blockchain** |
| GET | `/api/blocks` | Get blocks | - | `[{ block1 }, { block2 }]` |
| GET | `/api/blocks/:hash` | Get block | - | `{ hash, height, txs }` |
| **Transactions** |
| POST | `/api/transactions` | Submit transaction | `{ from, to, data }` | `{ txHash }` |
| GET | `/api/transactions/:hash` | Get transaction | - | `{ hash, status, ... }` |
| **Wallet** |
| GET | `/api/wallet/:address` | Get wallet info | - | `{ address, balance }` |
| POST | `/api/wallet/transfer` | Transfer coins | `{ to, amount }` | `{ txHash }` |
| **Contracts** |
| POST | `/api/contract/deploy` | Deploy contract | `{ wasm, initData }` | `{ contractId }` |
| POST | `/api/contract/execute` | Execute contract | `{ contractId, method, args }` | `{ result, txHash }` |
| GET | `/api/contract/:id` | Get contract state | - | `{ state, code }` |

---

## 🗄️ Data Models

### Frontend State (React Context)

```typescript
// WalletContext
{
  address: string,
  publicKey: string,
  privateKey: string,
  balance: number,
  isConnected: boolean
}

// GemContext
{
  myGems: Gem[],
  allGems: Gem[],
  selectedGem: Gem | null
}

// MarketplaceContext
{
  listings: Listing[],
  myListings: Listing[],
  recentSales: Sale[]
}
```

### Backend Models (MongoDB)

```typescript
// Gem Model
{
  _id: ObjectId,
  gemId: string,           // Unique gem ID
  owner: string,           // Wallet address
  color: string,           // Gem color
  rarity: string,          // Common, Rare, Epic, Legendary
  power: number,           // Gem power level
  attributes: {
    shine: number,
    size: number,
    ...
  },
  mintedAt: Date,
  mintTxHash: string,      // Blockchain transaction hash
  metadata: object
}

// Listing Model
{
  _id: ObjectId,
  listingId: string,
  gemId: string,
  seller: string,          // Wallet address
  price: number,
  status: string,          // active, sold, cancelled
  createdAt: Date,
  updatedAt: Date,
  txHash: string
}

// Transaction Model
{
  _id: ObjectId,
  txHash: string,
  from: string,
  to: string,
  type: string,            // mint, transfer, sale
  status: string,          // pending, confirmed, failed
  blockHeight: number,
  timestamp: Date,
  data: object
}
```

### Blockchain State (Rust)

```rust
// Block
struct Block {
    height: u64,
    hash: Hash,
    previous_hash: Hash,
    timestamp: u64,
    nonce: u64,
    difficulty: u64,
    transactions: Vec<Transaction>,
    merkle_root: Hash
}

// Transaction
struct Transaction {
    hash: Hash,
    from: Address,
    to: Address,
    value: u64,
    gas_limit: u64,
    gas_price: u64,
    nonce: u64,
    data: Vec<u8>,          // Contract call data
    signature: Signature
}

// NFT State (in contract)
struct Gem {
    id: String,
    owner: Address,
    metadata: HashMap<String, Value>,
    minted_at: u64
}

// Marketplace State (in contract)
struct Listing {
    id: String,
    gem_id: String,
    seller: Address,
    price: u64,
    active: bool
}
```

---

## 🔐 Security & Authentication

```
┌──────────────────────────────────────────────────────────────────────┐
│                         Security Layers                               │
└──────────────────────────────────────────────────────────────────────┘

Layer 1: Wallet Security
├─ Private key stored client-side only
├─ Ed25519 cryptographic signatures
├─ Address derived from public key hash
└─ Signature verification on every transaction

Layer 2: API Security
├─ JWT tokens for session management
├─ CORS protection (origin whitelist)
├─ Rate limiting (100 req/15min)
└─ Input validation & sanitization

Layer 3: Blockchain Security
├─ Proof of Work consensus
├─ Transaction signature verification
├─ Double-spend prevention
├─ Block validation (hash, difficulty)
└─ Merkle tree integrity

Layer 4: Smart Contract Security
├─ WASM sandboxed execution
├─ Gas metering (prevent infinite loops)
├─ State validation
└─ Access control (owner checks)

Layer 5: Network Security
├─ P2P encryption (libp2p)
├─ Peer authentication
├─ DDoS protection
└─ Bad peer blacklisting
```

---

## ⚡ Performance & Scalability

### System Capacity

```
┌────────────────────────────────────────────────────────────┐
│                    Performance Metrics                      │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Blockchain (nchain)                                       │
│  ├─ Block time: ~10 seconds (configurable)                │
│  ├─ TPS: ~100 transactions/second                         │
│  ├─ Block size: ~1 MB max                                 │
│  └─ Storage: ~1 GB per 100k blocks                        │
│                                                             │
│  Backend API                                               │
│  ├─ Requests: ~1000 req/second                            │
│  ├─ Response time: <100ms (cached)                        │
│  ├─ Response time: <500ms (blockchain)                    │
│  └─ Concurrent users: ~10,000                             │
│                                                             │
│  Frontend                                                  │
│  ├─ Load time: <2 seconds                                 │
│  ├─ Bundle size: ~500 KB (gzipped)                        │
│  └─ Real-time updates: WebSocket/polling                  │
│                                                             │
│  Database (MongoDB)                                        │
│  ├─ Read ops: ~5000 ops/second                            │
│  ├─ Write ops: ~1000 ops/second                           │
│  └─ Storage: ~10 GB per 1M gems                           │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

### Caching Strategy

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Frontend  │  Cache  │   Backend   │  Cache  │  Blockchain │
│   (React)   │ ◄─────► │  (MongoDB)  │ ◄─────► │   (nchain)  │
└─────────────┘         └─────────────┘         └─────────────┘
      │                       │                        │
      │                       │                        │
   Browser              In-Memory Cache          Blockchain
   Storage              (Redis optional)           State
      │                       │                        │
      │                       │                        │
   • User data            • Gem listings          • Source of
   • JWT tokens           • User profiles           truth
   • Recent gems          • Active listings       • Immutable
                          • TTL: 30-60 sec          records
```

---

## 🚀 Deployment Architecture

### Production Setup

```
┌──────────────────────────────────────────────────────────────────┐
│                      Production Environment                       │
└──────────────────────────────────────────────────────────────────┘

                         ┌─────────────────┐
                         │   Load Balancer │
                         │     (Nginx)     │
                         └────────┬────────┘
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
         ┌───────▼──────┐  ┌──────▼─────┐  ┌──────▼─────┐
         │  Frontend 1  │  │ Frontend 2 │  │ Frontend 3 │
         │  (React)     │  │  (React)   │  │  (React)   │
         │  Port 5173   │  │ Port 5174  │  │ Port 5175  │
         └──────────────┘  └────────────┘  └────────────┘
                                  │
                         ┌────────▼────────┐
                         │  API Gateway    │
                         │    (Express)    │
                         └────────┬────────┘
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
         ┌───────▼──────┐  ┌──────▼─────┐  ┌──────▼─────┐
         │  Backend 1   │  │ Backend 2  │  │ Backend 3  │
         │  (Node.js)   │  │ (Node.js)  │  │ (Node.js)  │
         │  Port 3000   │  │ Port 3001  │  │ Port 3002  │
         └──────┬───────┘  └─────┬──────┘  └─────┬──────┘
                │                │                │
                └────────────────┼────────────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
            ┌───────▼──────┐  ┌──▼──────┐  ┌─▼────────┐
            │  MongoDB     │  │ nchain  │  │ nchain   │
            │  Replica Set │  │ Node 1  │  │ Node 2   │
            └──────────────┘  └─────┬───┘  └────┬─────┘
                                    │           │
                                    └─────P2P───┘
```

---

## 📊 System Monitoring

### Health Checks

```
Frontend Health
├─ Page load time
├─ API response time
├─ Error rate
└─ Active users

Backend Health
├─ Request rate
├─ Response time (p50, p95, p99)
├─ Error rate
├─ Database connection pool
└─ Memory usage

Blockchain Health
├─ Block height
├─ Peer count
├─ Mempool size
├─ Mining hashrate
├─ Sync status
└─ Contract execution time

Database Health
├─ Query performance
├─ Connection count
├─ Storage usage
└─ Replication lag
```

---

This architecture provides a complete, scalable, and secure foundation for your Web3 NFT marketplace!
